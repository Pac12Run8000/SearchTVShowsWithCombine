import SwiftUI

struct AppColors {
    static let darkColor = Color(UIColor(red: 0.05, green: 0.05, blue: 0.05, alpha: 1.00)) // Dark color
    static let lightGrayColor = Color(UIColor(red: 0.90, green: 0.90, blue: 0.90, alpha: 1.00)) // Light gray color
    static let blueColor = Color(UIColor(red: 0.25, green: 0.35, blue: 0.85, alpha: 1.00)) // Blue color
    static let whiteColor = Color.white // White color
    static let blackColor = Color.black // Black color
}

struct AppStyles {
    static let textFieldPadding: CGFloat = 10
    static let buttonPadding: CGFloat = 10
    static let cornerRadius: CGFloat = 8
}
